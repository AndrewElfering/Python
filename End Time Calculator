# Evaluating end time of an event based on starting time and duration
hour = int(input("Starting time (hours): "))
mins = int(input("Starting time (minutes): "))
dura = int(input("Event duration (minutes): "))


print((int((hour * 60 + (mins + dura))/60)%24),":",((mins + dura)%60))
